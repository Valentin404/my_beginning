button.onclick = function () {
    const one = document.getElementById('one');
    if (navigator.userAgent.search(/Chrome/) > 0) {
      one.style.marginLeft = '25px' 
    }
}


// Я думаю этот метод только для уже существующих елементов. только корекция